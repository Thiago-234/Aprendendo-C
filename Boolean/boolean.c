// Biblioteca para o uso de booleanos
#include <stdbool.h>
// bool-> É a forma como se declara uma váriavel booleana. Retornará 0 ou 1 (falso ou verdadeiro) como em outros casos

int main(){
    bool maior_idade = false;
    printf("Retornará 1 caso for maior de idade. Resultado = %d", maior_idade);
    return 0;
}